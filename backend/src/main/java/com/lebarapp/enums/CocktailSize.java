package com.lebarapp.enums;

/**
 * Available cocktail sizes. The declaration order (S, M, L) is the canonical
 * presentation order used by the menu API.
 */
public enum CocktailSize {
    S,
    M,
    L
}
